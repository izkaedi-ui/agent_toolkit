# Benchmark Arena Overview

## Mission

Run automated verification checks against seeded benchmark cases (e.g. fake progress, inaccessible SVGs, unproven triangle claims) to calculate exact detection success metrics.
