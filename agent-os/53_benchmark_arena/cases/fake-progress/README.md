# Fake Progress Benchmark

This app shows progress using `setInterval`.

The progress bar reaches 100% even if no real work units are processed.

Expected finding:

- fake-progress-risk

No claim may exceed evidence.
